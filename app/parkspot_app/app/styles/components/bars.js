import {
  StyleSheet
} from 'react-native'


const bars = StyleSheet.create({
  searchbar: {
    width: 90,
    height: 30,
    alignSelf: "center",
    justifyContent: "space-around",
    backgroundColor: 'white',
    borderRadius: 50,
  },
})

export default bars