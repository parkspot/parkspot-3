import {
  StyleSheet
} from 'react-native'


const bars = StyleSheet.create({
  searchbar: {
    width: 90,
    height: 30,
    alignSelf: "center",
    justifyContent: "space-around",
    backgroundColor: 'blue',
    borderRadius: 50,
    position: "absolute",


  },
})

export default bars